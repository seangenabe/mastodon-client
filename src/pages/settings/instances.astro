---
import InstanceManager from "@/components/settings/instances/InstanceManager";
import SettingsLayout from "@/components/settings/SettingsLayout.astro";

export const name = "Instances";
export const order = -100;
---
<SettingsLayout name={name}>
  <InstanceManager client:only="solid-js" />
</SettingsLayout>
