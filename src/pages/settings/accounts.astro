---
import AccountManager from "@/components/settings/accounts/AccountManager";
import SettingsLayout from "@/components/settings/SettingsLayout.astro";

export const name = "Accounts";
export const order = -90;
---
<SettingsLayout name={name}>
  <AccountManager client:only="solid-js" />
</SettingsLayout>
